"""Novel CLI helpers (rendering, formatting).

This package hosts shared helpers used by the CLI subcommands in
``main.py``. Keeping the renderers here prevents ``main.py`` from
growing further and lets us unit-test them in isolation.
"""
